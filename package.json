{
    "name": "generator",
    "private": true,
    "version": "0.1.0",
    "description": "Rust Cloudflare Worker project",
    "scripts": {
        "build": "echo 'Rust build is handled by wrangler [build].command'",
        "deploy": "wrangler deploy"
    }
}