{
    "name": "generator",
    "private": true,
    "version": "0.1.0",
    "description": "Rust Cloudflare Worker project",
    "scripts": {
        "build:worker": "[ -x \"$HOME/.cargo/bin/worker-build\" ] || cargo install -q worker-build --version 0.7.5 && $HOME/.cargo/bin/worker-build --release",
        "build": "npm run build:worker",
        "deploy": "npm run build:worker && wrangler deploy"
    }
}